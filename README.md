# tempo
A Google Chrome Extension engineered. It transitions users from chaotic context switching (flicker) into focused work (flow). It autonomously monitors the user's active tabs during scheduled calendar events, learns contextual associations based on event keywords, and pre-loads the workspace for future similar events via an interactive notification.
